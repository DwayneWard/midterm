# Теоретическая часть контрольной работы. Вариант 5
## Разработка на Python. Углублённый

### Вопрос 1
Эндпоинт `POST /payments` возвращает 500. Логи показывают ошибку сериализации. Какие шаги помогут подтвердить, что проблема в валидации входящих данных? 

А) Изменить HTTP-метод на GET
Б) Перейти на формат XML вместо JSON
В) Валидировать JSON-данные перед сохранением дополнительно
Г) Проверить схемы Pydantic и добавить валидацию входных данных
Д) Повторять запросы до успешного выполнения

**Правильные ответы:** Г

**Проверяемый ОР:** Понимает. - Как реализовать API, взаимодействующее с фронтендом.



### Вопрос 2
Как сделать интеграцию API и фронта устойчивой? Отметь все подходящие варианты.

А) Писать интеграционные тесты  
Б) Логировать ошибки и добавлять request-id
В) Синхронизировать документацию и версии
Г) Использовать единый формат `{ "data": ..., "error": ... }`
Д) Всегда возвращать 200

**Правильные ответы:** А, В, Г

**Проверяемый ОР:** Понимает. - Как организовать эндпоинты для CRUD-операций и поддерживать согласованность API с фронтендом.



### Вопрос 3
В FastAPI добавлен обработчик:
```python
@app.exception_handler(HTTPException)
async def custom_handler(request, exc):
    return JSONResponse(
        status_code=exc.status_code,
        content={"error": exc.detail, "trace_id": request.state.trace_id},
    )
```
Для чего используется такой обработчик?

А) Чтобы скрывать все ошибки от клиента
Б) Чтобы централизованно форматировать ответы об ошибках и добавлять идентификатор запроса
В) Чтобы полностью отключить стандартные ответы FastAPI
Г) Чтобы логировать только успешные ответы

**Правильный ответ:** Б

**Проверяемый ОР:** Понимает. - Как кастомные обработчики ошибок помогают улучшить стабильность API.



### Вопрос 4
Какие типы данных могут передаваться через HTML-формы по умолчанию? Отметь все подходящие варианты.

А) JSON-объекты без дополнительной сериализации
Б) Текстовые строки
В) Файлы (с `multipart/form-data`)
Г) Числовые значения

**Правильные ответы:** Б, В, Г

**Проверяемый ОР:** Знает. - Какие типы данных могут передаваться через формы.



### Вопрос 5
Какой код используется для подключения статических файлов в FastAPI?

А) `app.mount("/static", StaticFiles(directory="static_src"), name="static")`
Б) `app.serve_static("static_src")`
В) `app.static("/static")`
Г) `app.include_router("/static")`

**Правильный ответ:** А

**Проверяемый ОР:** Знает. - Различия между статическими файлами и шаблонами, способы их подключения в FastAPI.



### Вопрос 6
В Jinja2-шаблоне используется переменная `{{ username }}`, но она не передана в контекст. Что увидит пользователь на странице?

А) В месте подстановки переменной будет пустая строка
Б) FastAPI выбросит исключение при рендеринге шаблона
В) В шаблоне отобразится текст `{{ username }}` как есть
Г) Jinja2 автоматически подставит значение по умолчанию

**Правильный ответ:** А

**Проверяемый ОР:** Понимает. - Как передавать данные в шаблоны через Jinja2.



### Вопрос 7
Какие шаги помогают организовать структуру проекта FastAPI для масштабирования? Отметь все подходящие варианты.

А) Хранить все обработчики в одном файле `main.py` для простоты навигации
Б) Разделить бизнес-логику, модели данных и вспомогательные функции по отдельным пакетам
В) Настроить общие настройки приложения в отдельном модуле  
Д) Использовать один роутер для всех эндпоинтов без группировки
Г) Вынести логические группы эндпоинтов в отдельные модули и подключить их через `APIRouter`

**Правильные ответы:** Б, В, Г

**Проверяемый ОР:** Понимает. - Как организовать структуру проекта на FastAPI.



### Вопрос 8
Эндпоинт `PUT /users/{id}` успешно обновил существующую запись без тела ответа. Какой статус-код подходит? Напиши число.

**Правильный ответ:** `204`

**Проверяемый ОР:** Знает. - Принципы валидации данных в API.



### Вопрос 9
В коде есть декоратор `@router.post("/sync")`, но функция-обработчик отсутствует. Что произойдёт при запуске приложения?

А) Эндпоинт будет игнорироваться, запросы вернут 404
Б) FastAPI автоматически создаст обработчик, возвращающий пустой ответ
В) FastAPI подставит обработчик по умолчанию, возвращающий `{"message": "OK"}`
Г) Приложение упадёт с ошибкой при запуске

**Правильный ответ:** Г

**Проверяемый ОР:** Знает. - Основные концепции маршрутизации (GET, POST).



### Вопрос 10
Какие шаги необходимы для передачи данных в Jinja2-шаблон в FastAPI? Отметь все подходящие варианты.

А) Вызвать `templates.TemplateResponse("stats.html", {"request": request, "report": report})`
Б) Оставить шаблон без данных — они подставятся автоматически
В) Передать данные через словарь в параметр `context`
Г) Использовать глобальные переменные вместо контекста

**Правильные ответы:** А, В

**Проверяемый ОР:** Знает. - Основы работы с Jinja2 в FastAPI: передача данных, использование циклов и условий.



### Вопрос 11
Почему API описывают в контракте перед интеграцией?

А) Чтобы автоматизировать создание мок-данных для тестов
Б) Чтобы фронтенд и бэкенд одинаково понимали формат запросов и ответов
В) Чтобы описать структуру данных в базе для интеграции
Г) Чтобы ограничить использование API только веб-браузерами

**Правильный ответ:** Б

**Проверяемый ОР:** Понимает. - Роль API в обмене данными между клиентом и сервером.



### Вопрос 12
Эндпоинт FastAPI читает данные через `request.json()`, но клиент отправил запрос с заголовком `Content-Type: application/x-www-form-urlencoded`. Что вернёт FastAPI?

А) Автоматически переключится на чтение формы через `request.form()`
Б) Автоматически преобразует форму в JSON и продолжит обработку
В) Вернёт 422 или 400 из-за несоответствия формата тела
Г) Попытается парсить тело как JSON и вернёт ошибку парсинга

**Правильный ответ:** В

**Проверяемый ОР:** Понимает. - Как обрабатывать входящие данные в FastAPI.



### Вопрос 13
Назови три HTTP-метода, которые чаще всего применяют для чтения и удаления данных. Напиши через запятую.

**Правильный ответ:** `GET, HEAD, DELETE`

**Проверяемый ОР:** Знает. - Основные компоненты HTTP-запросов и ответов (методы, заголовки, тело запроса, коды ответов).



### Вопрос 14
Сервер не проверил данные формы и сохранил пустое поле `phone` в базу данных. Какие последствия возможны?

А) Ничего критического — пустая строка является валидным значением
Б) Некорректными данными в базе и ошибками при дальнейшей обработке
В) Автоматическим заполнением поля через FastAPI
Г) Автоматической повторной отправкой формы браузером

**Правильный ответ:** Б

**Проверяемый ОР:** Понимает. - Как корректная обработка данных из форм улучшает взаимодействие пользователя с приложением и повышает безопасность.



### Вопрос 15
Какие три ключевых поля должны быть включены в структурированный JSON-ответ об ошибке? Напиши их через запятую.

**Правильный ответ:** `code, message, details` (принимаются аналоги вроде `status, error, info`)

**Проверяемый ОР:** Понимает. - Как клиент и сервер взаимодействуют через HTTP-запросы и ответы.



### Вопрос 16
Какая информация входит в структуру HTTP-запроса? Отметь все подходящие варианты.

А) URI / путь
Б) Метод
В) Тело
Г) Заголовки

**Правильные ответы:** А, Б, В, Г

**Проверяемый ОР:** Знает. - Основные компоненты HTTP-запросов и ответов (методы, заголовки, тело запроса, коды ответов).



### Вопрос 17
Эндпоинт при любой ошибке возвращает статус 200 с сообщением «error». Какие изменения нужно внести, чтобы клиенты корректно обрабатывали разные сценарии? Отметь все подходящие варианты.

А) Возвращать статусы 4xx для ошибок клиента и 5xx для ошибок сервера
Б) Включать в ответ структурированное сообщение об ошибке
В) Документировать возможные HTTP-статусы, которые может вернуть эндпоинт
Г) Сохранять статус 200, чтобы фронтенд не показывал ошибки пользователю

**Правильные ответы:** А, Б, В

**Проверяемый ОР:** Понимает. - Как корректно обрабатывать ошибки в API.


### Вопрос 18
В веб-приложении `GET /reports` возвращает `503 Service Unavailable`, потому что сервис занят обслуживанием. Что показать пользователю?

А) Автоматический переход на страницу статуса сервиса
Б) Отображение последних закэшированных данных с пометкой
В) Индикатор загрузки без дополнительной информации о причине
Г) Сообщение о временной недоступности и предложение попробовать позже

**Правильный ответ:** Г

**Проверяемый ОР:** Понимает. - Как клиент и сервер взаимодействуют через HTTP-запросы и ответы.



### Вопрос 19
API вернул ответ:
```json
{
  "detail": [
    {"loc": ["body", "amount"], "msg": "value is not a valid float"}
  ]
}
```
Что означает такой ответ?

А) Ответ сгенерирован промежуточным прокси
Б) Это HTML-страница
В) Ошибка сервера FastAPI
Г) Ошибка клиента (некорректные данные в запросе)

**Правильный ответ:** Г

**Проверяемый ОР:** Понимает. - Как на клиенте различать ошибки сервера (500) и ошибки клиента (400).



### Вопрос 20
Зачем держать фронтенд и бэкенд раздельными в проекте? Отметь все подходящие причины.

А) Использовать разные стеки технологий
Б) Скрывать бизнес-логику на сервере
В) Минимизировать количество HTTP-запросов путём объединения
Г) Масштабировать части независимо

**Правильные ответы:** А, Б, Г

**Проверяемый ОР:** Понимает. - Почему важно разделение фронтенда и бэкенда в современных приложениях.



### Вопрос 21
Фронтенд получает `307` на `/login`. Что это означает?

А) База данных недоступна
Б) Сервер перезапускается
В) Клиент обратился не к тому домену
Г) Сессия истекла, нужно залогиниться

**Правильный ответ:** Г

**Проверяемый ОР:** Понимает. - Как клиент и сервер взаимодействуют через HTTP-запросы и ответы.



### Вопрос 22
Эндпоинт `GET /files/{file_id}` объявлен с параметром `file_id: int`. Какой статус-код вернёт FastAPI, если клиент запросит `/files/report`? Напиши число.

**Правильный ответ:** `422`

**Проверяемый ОР:** Понимает. - Как обрабатывать запросы и возвращать ответы.



### Вопрос 23
Какие шаги необходимы для обеспечения корректной обработки JSON-ответов API клиентом? Отметь все подходящие варианты.

А) Указывать заголовок `Content-Type: application/json`
Б) Добавлять подробные логи сервера в тело ответа для отладки
В) Документировать API-контракт в Swagger/OpenAPI  
Д) Возвращать ошибки с описанием проблемных полей
Г) Использовать единый формат тела ответа, например `{ "data": ... }`

**Правильные ответы:** А, В, Г

**Проверяемый ОР:** Понимает. - Как реализовать API, взаимодействующее с фронтендом.



### Вопрос 24
В архитектуре SPA с отдельным бэкендом какие задачи выполняет бэкенд? Отметь все подходящие варианты.

А) Обработка бизнес-логики
Б) Хранение данных и интеграции
В) Генерация статических HTML-страниц с применением стилей
Г) Обработка событий пользовательского интерфейса

**Правильные ответы:** А, Б

**Проверяемый ОР:** Знает. - Различие между фронтендом и бэкендом, их задачи и роли.



### Вопрос 25
Эндпоинт должен принимать query-параметр `source` из URL `/sources?source=api`. Какие объявления параметра функции-обработчика подходят? Отметь все подходящие варианты.

А) `def list_sources(source: str = "/sources?source")`
Б) `def list_sources(source: str = Query(None))`
В) `def list_sources(source: str = Path(...))`
Г) `def list_sources(source: str | None = None)`

**Правильные ответы:** Б, Г

**Проверяемый ОР:** Понимает. - Разницу между query и path parameters и их назначение.



### Вопрос 26
API возвращает 500 из-за временной недоступности базы данных. Какие шаги помогут улучшить обработку ошибок и дать фронтенду понятную обратную связь? Отметь все подходящие варианты.

А) Возвращать статус 503 с сообщением о временной недоступности
Б) Скрывать ошибку и возвращать статус 200  
Д) Согласовать с фронтендом формат отображения ошибок пользователю
В) Оставить статус 500 и возвращать пустое тело ответа
Г) Добавить обработчик исключений, возвращающий структурированный JSON и логирующий ошибку

**Правильные ответы:** А, Г

**Проверяемый ОР:** Понимает. - Как кастомные обработчики ошибок помогают улучшить стабильность API.

