# Теоретическая часть контрольной работы
## Вариант 2

**Дисциплина:** Разработка на Python. Основной

**Фамилия, Имя:** _________________________________

---

### Вопрос 1
Перед релизом ты проверяешь ветку `feature/payments`: в `main` уже есть три новых фикса, а твоя ветка отстаёт. Что сделает команда `git pull --rebase origin main`, если ты стоишь в `feature/payments`?

- [ ] Откатит ветку к состоянию `main` на момент последнего rebase
- [ ] Подтянет коммиты из `main` и перепишет твою историю поверх них
- [ ] Создаст merge-коммит между `feature/payments` и `main`
- [ ] Удалит все твои коммиты, оставив только `main`

### Вопрос 2
Твой `git status` сообщает `Your branch is ahead of 'origin/docs' by 2 commits`. Что это значит и что нужно сделать, чтобы поделиться изменениями?

- [ ] Удалённая ветка опережает локальную; надо сделать `git pull`
- [ ] Твои коммиты конфликтуют с коммитами в удаленном репозитории и будут отклонены
- [ ] Локальная ветка содержит два коммита, которых нет в удалённой ветке, и нужно выполнить `git push`
- [ ] Коммиты уже есть на сервере; нужно только локально их проверить

### Вопрос 3
На демо заказчик просит быстро откатить последний коммит, потому что фича ведёт себя нестабильно. Нужно вернуть ветку к предыдущему состоянию, сохранив историю изменений. Какой командой это сделать?

**Ответ:**

_________________________________


### Вопрос 4
Команда готовит релиз и просит убрать временные файлы IDE (например, `.idea/`) из истории репозитория. Какие действия помогут Git перестать их отслеживать? Отметь все подходящие варианты.

- [ ] Добавить шаблон в `.gitignore` и удалить файлы из индекса командой `git rm --cached`
- [ ] Перенести файлы в папку `.git`
- [ ] Переименовать файлы

### Вопрос 5
Перед публикацией Merge Request ты проверяешь историю. Какой сценарий лучше помогает ревьюеру быстро понять изменения?

- [ ] Один большой коммит на все изменения
- [ ] Один большой коммит с сообщением и подробное описание изменений в MR
- [ ] 2 коммита на большую фичу, но с хорошим кодом
- [ ] Серия осмысленных коммитов с понятными сообщениями

### Вопрос 6
Разработчик хочет проверить, кто последний правил файл `src/api.py` и когда это было. Какой командой вывести историю изменений по этому файлу?

**Ответ:**

_________________________________


### Вопрос 7
Создаёшь новый проект в GitLab для команды, которая подключится через неделю. Какие разделы README помогут коллегам быстро включиться в работу? Отметь все подходящие варианты.

- [ ] Ссылку на релевантную документацию или API
- [ ] Инструкции по настройке окружения
- [ ] Цель проекта и основные команды запуска
- [ ] Информация о том, какие данные по проекту есть у команды разработки

### Вопрос 8
Клиент отправляет POST-запрос для создания нового пользователя, и сервер возвращает `HTTP/1.1 201 Created`. Как правильно интерпретировать этот ответ?

- [ ] Запрос обработан успешно, ресурс создан, но сервер перенаправит клиента на другую страницу
- [ ] Запрос обработан успешно, ресурс создан, но нужно отправить дополнительный запрос для подтверждения
- [ ] Запрос обработан успешно, но ресурс не был создан, нужно повторить запрос
- [ ] Запрос обработан успешно, новый ресурс создан на сервере, можно работать дальше

### Вопрос 9
Разработчик проектирует REST-эндпоинт для обновления профиля пользователя. Нужно полностью обновить данные пользователя по его идентификатору. Какой маршрут и метод соответствуют принципам REST?

- [ ] POST `/api/profile/update` с телом `{"user_id": 123, ...}`
- [ ] GET `/api/profile/{user_id}` с телом запроса
- [ ] PATCH `/api/profile/{user_id}` с телом запроса, содержащим все поля профиля
- [ ] PUT `/api/profile/{user_id}` с телом запроса, содержащим все поля профиля

### Вопрос 10
Инженер настроил `PUT /orders/123` так, что на успешное обновление отправляет `204 No Content`. Нужно понять, корректно ли использование этого статус-кода для операции обновления.

- [ ] Да, 204 означает, что операция прошла успешно, но сервер не возвращает тело ответа — это подходит для обновления
- [ ] Нет, для обновления всегда нужно возвращать 200 OK с обновлёнными данными в теле ответа
- [ ] Нет, PUT должен возвращать 201 Created, так как ресурс был изменён
- [ ] Нет, нужно использовать 200 OK без тела ответа, но не 204

### Вопрос 11
Разработчик изучает структуру HTTP-запроса. Какие элементы обязательно входят в базовую структуру HTTP-запроса? Отметь все подходящие варианты.

- [ ] Стартовая строка с методом и URI
- [ ] Тело запроса (если нужно, например для POST)
- [ ] Статус-код ответа
- [ ] Заголовки (минимум Host)

### Вопрос 12
Разработчик тестирует API в Postman и получает 404 на `GET /api/items/777`. Нужно понять причину ошибки и правильно организовать процесс диагностики. Какие шаги помогут уточнить, что произошло? Отметь все подходящие варианты.

- [ ] Попробовать другой ID ресурса, чтобы проверить, работает ли эндпоинт вообще
- [ ] Проверить корректность URL и параметров пути
- [ ] Изучить документацию API и посмотреть, какие статусы возвращает эндпоинт для несуществующих ресурсов
- [ ] Убедиться, что ресурс с ID 777 существует в системе

### Вопрос 13
Разработчик отправляет форму входа через `requests.post`. Нужно передать данные `{"email": "...", "password": "..."}` в теле запроса с типом `application/json`. Как правильно организовать процесс формирования запроса?

- [ ] Использовать `requests.post(url, json=data)`
- [ ] Использовать `requests.post(url, files=data)`
- [ ] Использовать `requests.post(url, data=data)`
- [ ] Использовать `requests.post(url, params=data)`

### Вопрос 14
Ответ API:
```json
{"status": "ok", "payload": {"items": [{"id": 1, "name": "Laptop"}]}}
```
Как получить название первого товара, если данные лежат в словаре `data`?

**Ответ:**

_________________________________


### Вопрос 15
Как запросить JSON-ответ и сразу получить Python-словарь через библиотеку `requests`?

- [ ] `response = requests.get(url); data = response.json()`
- [ ] `response = requests.get(url); data = response.data()`
- [ ] `response = requests.get(url); data = response.json`
- [ ] `response = requests.get(url); data = response.text()`

### Вопрос 16
Проект разбит на модули `api`, `services`, `utils`, но при импорте появились ошибки из-за циклических зависимостей. Какие шаги помогут устранить проблему? Отметь все подходящие варианты.

- [ ] Переместить все импорты в конец файлов, чтобы модули загружались после определения функций
- [ ] Использовать абсолютные импорты вместо относительных, чтобы изменить порядок загрузки
- [ ] Вынести общую логику в отдельный модуль, чтобы разорвать цикл зависимостей
- [ ] Выполнить импорт внутри функций там, где это возможно, чтобы отложить загрузку модуля

### Вопрос 17
В каталоге `helpers/` появился файл `email.py`, а в проекте уже есть модуль `email` из стандартной библиотеки. Как избежать конфликтов при импорте?

- [ ] Переименовать локальный модуль, чтобы не совпадал с системным
- [ ] Использовать alias при импорте стандартного модуля: `import email as std_email`
- [ ] Переместить локальный модуль в другую директорию и использовать абсолютный импорт
- [ ] Использовать относительный импорт для локального модуля: `from .helpers import email`

### Вопрос 18
Проект организован по модульному подходу. Какие преимущества даёт модульный подход для поддержки сервиса? Отметь все подходящие варианты.

- [ ] Упрощает расширение функционала без риска сломать существующий код
- [ ] Упрощает тестирование отдельных компонентов без необходимости поднимать всю систему
- [ ] Упрощает поддержку кода благодаря изоляции изменений в отдельных модулях
- [ ] Ускоряет выполнение программы за счёт оптимизации интерпретатором

### Вопрос 19
После создания объекта бота на библиотеке `telebot` что нужно сделать, чтобы бот начал отвечать?

- [ ] Вызвать `bot.polling()` для запуска бота
- [ ] Отправить тестовое сообщение боту в Telegram
- [ ] Настроить webhook и указать URL сервера
- [ ] Зарегистрировать обработчики команд

### Вопрос 20
Некоторые пользователи пишут `/help`, но бот молчит. Что проверить в первую очередь? Отметь все подходящие варианты.

- [ ] Запущен ли бот через `bot.polling()`
- [ ] Корректно ли указан токен бота
- [ ] Не падает ли бот из-за исключений
- [ ] Зарегистрирован ли обработчик команды `/help`

### Вопрос 21
Нужны разные ответы на текстовые сообщения и команды. Как настроить обработчики в `telebot`?

- [ ] Один обработчик с условиями внутри функции справится со всеми случаями
- [ ] Зарегистрировать обработчики сначала для команд, затем для текста
- [ ] Использовать один обработчик для всех сообщений и проверять `message.text.startswith('/')` внутри функции
- [ ] Зарегистрировать обработчики сначала для текста, затем для команд

### Вопрос 22
Пользователь нажимает inline-кнопку «Оплатить», а бот должен провести проверку заказа перед подтверждением. Как это реализовать?

- [ ] Сохранить идентификатор заказа в глобальной переменной и обработать нажатие в `@bot.message_handler()`
- [ ] В `callback_data` передать идентификатор заказа и обработать его в `@bot.callback_query_handler`
- [ ] Использовать `ReplyKeyboardMarkup` вместо inline-кнопок и обработать выбор через `@bot.message_handler()`
- [ ] Передать идентификатор заказа через параметр `message.text` и обработать в обработчике текстовых сообщений

