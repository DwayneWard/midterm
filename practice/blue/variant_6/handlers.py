"""
Модуль с обработчиками команд и сообщений бота.
"""
import telebot
from telebot import types

from api_client import get_recipe
from storage import get_user_favorites, add_favorite_meal, remove_favorite_meal

def register_handlers(bot: telebot.TeleBot):
    """
    Регистрация всех обработчиков команд и сообщений.
    
    TODO: Реализовать функцию:
    1. Зарегистрировать обработчик команды /start используя декоратор @bot.message_handler(commands=['start'])
       Функция-обработчик должна вызывать start_handler(bot, message)
    
    2. Зарегистрировать обработчик команды /recipe используя декоратор @bot.message_handler(commands=['recipe'])
       Функция-обработчик должна вызывать recipe_handler(bot, message)
    
    3. Зарегистрировать обработчик команды /favorites используя декоратор @bot.message_handler(commands=['favorites'])
       Функция-обработчик должна вызывать favorites_handler(bot, message)
    
    4. Зарегистрировать обработчик текстовых сообщений используя декоратор @bot.message_handler(func=lambda message: True)
       Функция-обработчик должна вызывать recipe_handler(bot, message)
    
    5. Зарегистрировать обработчик callback-запросов от inline-кнопок используя декоратор @bot.callback_query_handler(func=lambda call: True)
       Функция-обработчик должна вызывать button_callback_handler(bot, call)
    """
    pass


def start_handler(bot: telebot.TeleBot, message: types.Message):
    """
    Обработчик команды /start.
    
    TODO: Реализовать обработчик:
    1. Отправить приветственное сообщение пользователю используя bot.send_message()
    2. Сообщение должно содержать информацию о доступных командах:
       - /recipe <название> - получить рецепт блюда (например, /recipe pasta)
       - /favorites - показать избранные блюда
    """
    pass


def recipe_handler(bot: telebot.TeleBot, message: types.Message):
    """
    Обработчик команды /recipe и текстовых сообщений с названием блюда.
    
    TODO: Реализовать обработчик:
    1. Получить название блюда:
       - Если это команда /recipe, взять название из message.text (разделить по пробелам и взять второй элемент, если есть)
       - Если это текстовое сообщение, взять весь message.text как название блюда
    
    2. Если название не указано (пустая строка или только команда), отправить сообщение: 
       "Пожалуйста, укажите название блюда"
    
    3. Вызвать get_recipe(meal_name) для получения данных о рецепте
    
    4. Если данные получены успешно (не None):
       - Извлечь из ответа: 
         * Название: data['strMeal']
         * Категория: data['strCategory']
         * Кухня: data['strArea']
         * Инструкции: data['strInstructions'] (можно взять первые 200 символов)
         * Ингредиенты: собрать из полей strIngredient1, strIngredient2, ... (до strIngredient20, где есть значения)
       - Сформировать сообщение вида:
         "Рецепт: {название}
         Категория: {категория}
         Кухня: {кухня}
         
         Ингредиенты:
         {список ингредиентов}
         
         Инструкции:
         {первые 200 символов инструкций}..."
       - Создать inline-кнопку "Добавить в избранное"
         callback_data должен быть f"add_{meal_name}" (заменить пробелы на подчеркивания)
       - Создать клавиатуру и добавить кнопку
       - Отправить сообщение с клавиатурой
    
    5. Если данные не получены (None):
       - Отправить сообщение: "Не удалось найти рецепт. Проверьте название блюда."
    
    6. Обработать возможные исключения используя try/except
    """
    pass


def favorites_handler(bot: telebot.TeleBot, message: types.Message):
    """
    Обработчик команды /favorites - показывает список избранных блюд.
    
    TODO: Реализовать обработчик:
    1. Получить user_id
    
    2. Получить список избранных блюд через соответсвующий функционал
    
    3. Если список пуст:
       - Отправить сообщение: "У вас пока нет избранных блюд. Используйте /recipe для добавления."
    
    4. Если список не пуст:
       - Создать inline-клавиатуру
       - Для каждого блюда создать кнопку:
         * Текст кнопки - название блюда
         * callback_data для каждой кнопки: f"recipe_{meal_name}" (заменить пробелы на подчеркивания)
       - Добавить кнопку "Очистить все" с callback_data="clear_all"
       - Отправить сообщение: "Ваши избранные блюда:" с клавиатурой
    """
    pass


def button_callback_handler(bot: telebot.TeleBot, call: types.CallbackQuery):
    """
    Обработчик нажатий на inline-кнопки.
    
    TODO: Реализовать обработчик:
    1. Получить callback_data из call.data
    
    2. Обработать разные типы callback_data:
       a) Если callback_data начинается с "add_":
          - Извлечь название блюда из callback_data (убрать префикс "add_", заменить подчеркивания на пробелы)
          - Получить user_id
          - Вызвать add_favorite_meal(user_id, meal_name)
          - Если успешно: ответить "Блюдо {meal_name} добавлено в избранное"
          - Если уже было: ответить "Блюдо {meal_name} уже в избранном"
       
       b) Если callback_data начинается с "recipe_":
          - Извлечь название блюда из callback_data (убрать префикс "recipe_", заменить подчеркивания на пробелы)
          - Вызвать get_recipe(meal_name)
          - Если данные получены, сформировать сообщение как в recipe_handler
          - Отправить сообщение с данными о рецепте
          - Создать кнопку "Добавить в избранное" если блюда ещё нет в избранном
       
       c) Если callback_data равен "clear_all":
          - Получить user_id
          - Получить список блюд через соответсвующий функционал
          - Для каждого блюда вызвать remove_favorite_meal(user_id, meal_name)
          - Ответить: "Все блюда удалены из избранного"
    
    3. После обработки вызвать bot.answer_callback_query(call.id) чтобы убрать индикатор загрузки
    
    4. Обработать возможные исключения используя try/except
    """
    pass

